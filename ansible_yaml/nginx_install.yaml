---
 - name: install nginx
   hosts: nginx_server
   become: yes 
  
   tasks:
    - name: install nginx server
      apt:
       name: nginx
       state: present
    
    - name: service 
      service: 
       name: nginx
       state: started     