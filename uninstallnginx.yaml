---
 - name: uninstall nginx completly
   hosts: remote_server
   become: yes 
  
   tasks:
    - name: uninstall completly
      shell: sudo apt-get remove nginx nginx-common -y.
      shell: sudo apt-get purge nginx nginx-common -y
      shell: sudo apt-get autoremove -y
   